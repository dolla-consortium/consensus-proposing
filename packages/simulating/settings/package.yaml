name: dolla-consensus-proposing-simulating-settings
version: 0.0.0.1

ghc-options:
  - -Wall

library:
  source-dirs: library

  dependencies:
    - dolla-base == 1.0.0
    - base == 4.13.0.0
    - aeson == 1.4.7.1
    - dolla-consensus-proposing-receptioning-settings


